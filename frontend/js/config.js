/* ============================================================
 * config.js — Frontend тохиргоо
 *   API_BASE-г development-д http://localhost:3000/api гэж сольж
 *   болно.
 * ============================================================ */

export const API_BASE = 'https://airguide-production-ec87.up.railway.app/api';

/** Хайлтад зориулсан тохиргоо */
export const SEARCH_CONFIG = {
  autocompleteDebounceMs: 200,
  autocompleteLimit: 8,
  searchMaxPassengers: 9
};
